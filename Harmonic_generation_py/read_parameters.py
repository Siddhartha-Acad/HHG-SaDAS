from parameters import (
    evolving_atom, SAE_model, n, l, m, total_states, N, r_max, L_map, confined, confinement_model,
    l_max, k_max, dt
)

print(evolving_atom, SAE_model, n, l, m, total_states, N, r_max, L_map, confined, confinement_model, l_max, k_max, dt)
