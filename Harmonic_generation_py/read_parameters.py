from parameters import (
    evolving_atom, SAE_model, l, total_states, N, r_max, L_map, confined, confinement_model
)

print(evolving_atom, SAE_model, l, total_states, N, r_max, L_map, confined, confinement_model)

